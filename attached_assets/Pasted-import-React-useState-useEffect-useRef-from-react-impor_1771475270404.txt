import React, { useState, useEffect, useRef } from 'react';
import {
Menu, X, ArrowRight, Calendar, Users,
Palette, FileText, Send, Globe, ChevronRight,
Sparkles, Zap, ZapOff
} from 'lucide-react';
/**
•  STYLE A: Spatial, Glass, Mesh, Elegant
•  STYLE B: Brutalist, Kinetic, Raw, High-Contrast
•  •  PATTERN ROTATION:
•  1.  Hero: Structure A + Style B
•  2.  Portal: Structure B + Style A
•  3.  Events: Structure B + Style A
•  4.  Spotlight: Structure A + Style B
•  5.  CTA: Mixed A/B
*/
// Custom styles for specific A/B effects
const styles = `
@keyframes fluid {
0% { background-position: 0% 50%; }
50% { background-position: 100% 50%; }
100% { background-position: 0% 50%; }
}
.mesh-gradient {
background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
background-size: 400% 400%;
animation: fluid 15s ease infinite;
}
.glass-card {
background: rgba(255, 255, 255, 0.05);
backdrop-filter: blur(12px);
border: 1px solid rgba(255, 255, 255, 0.1);
box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
}
.brutalist-border {
border: 3px solid black;
box-shadow: 8px 8px 0px 0px rgba(0,0,0,1);
}
.kinetic-text {
font-family: 'Inter', sans-serif;
font-weight: 900;
text-transform: uppercase;
letter-spacing: -0.05em;
line-height: 0.85;
}
.glitch-hover:hover {
animation: glitch 0.3s cubic-bezier(.25,.46,.45,.94) both infinite;
}
@keyframes glitch {
0% { transform: translate(0) }
20% { transform: translate(-2px, 2px) }
40% { transform: translate(-2px, -2px) }
60% { transform: translate(2px, 2px) }
80% { transform: translate(2px, -2px) }
100% { transform: translate(0) }
}
`;
const Nav = () => (
  <div className="hidden md:flex gap-8 text-sm font-bold uppercase tracking-widest text-black">
    <a href="#" className="hover:line-through">Events</a>
    <a href="#" className="hover:line-through">Projects</a>
    <a href="#" className="hover:line-through">Artists</a>
    <a href="#" className="hover:line-through">Submit</a>
  </div>

  <button className="bg-black text-white px-5 py-2 rounded-full text-xs font-black uppercase hover:scale-105 transition-transform">
    Join Now
  </button>
</div>

const Hero = () => (
<div className="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
  <div className="lg:col-span-7 z-10">
    {/* Style B: Kinetic Type */}
    <h1 className="kinetic-text text-7xl md:text-9xl lg:text-[11rem] mb-8 text-black">
      UNLEASH<br />
      <span className="text-transparent" style={{ WebkitTextStroke: '2px black' }}>CREATIVITY</span>
    </h1>
    
    <p className="text-xl max-w-lg mb-10 text-gray-800 font-medium">
      A decentralized sanctuary for visual artists, poets, and creative disruptors. Build, showcase, and evolve.
    </p>

    <div className="flex flex-wrap gap-4">
      {/* A+B Hybrid Button */}
      <button className="brutalist-border bg-[#c4ff00] px-8 py-4 text-black font-black uppercase text-lg hover:translate-x-1 hover:translate-y-1 hover:shadow-none transition-all">
        Explore Works
      </button>
      <button className="px-8 py-4 glass-card rounded-none border-2 border-black font-bold text-black hover:bg-black hover:text-white transition-all">
        Get Involved
      </button>
    </div>
  </div>

  <div className="lg:col-span-5 relative">
    {/* Style A: Spatial floating images */}
    <div className="relative w-full aspect-square glass-card rounded-3xl overflow-hidden rotate-3 hover:rotate-0 transition-transform duration-700 shadow-2xl">
      <img 
        src="https://images.unsplash.com/photo-1541701494587-cb58502866ab?auto=format&fit=crop&q=80&w=800" 
        className="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-1000"
        alt="Art piece"
      />
    </div>
    <div className="absolute -bottom-10 -left-10 w-48 h-48 glass-card rounded-2xl overflow-hidden -rotate-6 shadow-xl hidden md:block">
       <img 
        src="https://images.unsplash.com/photo-1579783902614-a3fb3927b6a5?auto=format&fit=crop&q=80&w=400" 
        className="w-full h-full object-cover"
        alt="Art piece"
      />
    </div>
  </div>
</div>

const UserGalleryPortal = () => (
const EventsSection = () => {
const events = [
{ title: "Free Webinar", type: "Digital Mastery", color: "bg-blue-400" },
{ title: "Design Consult", type: "Portfolio Review", color: "bg-[#c4ff00]" },
{ title: "Mural Project", type: "Community Build", color: "bg-orange-400" }
];
return (
<section className="py-24 bg-gray-50 overflow-hidden">
<div className="max-w-7xl mx-auto px-4">
<div className="flex items-end justify-between mb-16">
<h2 className="kinetic-text text-5xl md:text-7xl">Upcoming<br/>Action</h2>
<div className="text-right hidden md:block">
<p className="font-bold text-gray-400">01 / 02 / 03</p>
</div>
</div>
    {/* Structure B: Broken Grid */}
    <div className="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-0">
      {events.map((event, idx) => (
        <div 
          key={idx}
          className={`
            group relative glass-card p-10 min-h-[350px] flex flex-col justify-between
            ${idx === 0 ? 'md:translate-y-12 z-10' : ''}
            ${idx === 1 ? 'md:-translate-y-8 z-20 brutalist-border !bg-white' : ''}
            ${idx === 2 ? 'md:translate-y-20 z-10' : ''}
          `}
        >
          <div>
            <span className={`inline-block px-3 py-1 text-[10px] font-black uppercase mb-4 ${event.color} text-black`}>
              {event.type}
            </span>
            <h3 className="text-4xl font-black uppercase leading-none text-black">
              {event.title}
            </h3>
          </div>
          
          <button className="flex items-center gap-2 font-bold text-sm uppercase hover:gap-4 transition-all">
            Book Slot <ArrowRight className="w-4 h-4" />
          </button>
        </div>
      ))}
    </div>
  </div>
</section>

);
};
const ArtistSpotlight = () => (
    {/* Large Feature - Style B (Collage/Raw) */}
    <div className="md:col-span-8 brutalist-border relative overflow-hidden group">
      <img 
        src="https://images.unsplash.com/photo-1501196354995-cbb51c65aaea?auto=format&fit=crop&q=80&w=1200" 
        className="w-full h-full object-cover"
        alt="Artist spotlight"
      />
      <div className="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
        <button className="bg-white text-black px-12 py-4 font-black uppercase text-xl">
          View Profile
        </button>
      </div>
      <div className="absolute top-6 left-6 bg-[#c4ff00] px-4 py-2 font-black uppercase text-sm brutalist-border">
        Artist of the Month
      </div>
      <div className="absolute bottom-6 right-6 text-white text-right z-10">
        <h4 className="text-6xl font-black uppercase leading-tight">MARCUS<br/>KAIRO</h4>
      </div>
    </div>

    {/* Small Panels - Style A (Glass/Structured) */}
    <div className="md:col-span-4 grid grid-rows-2 gap-6">
      <div className="glass-card rounded-3xl p-8 flex flex-col justify-center bg-gray-900 text-white">
        <p className="text-sm uppercase font-bold text-gray-400 mb-2">Ongoing Projects</p>
        <h5 className="text-4xl font-black mb-4">Concrete Dreams</h5>
        <div className="flex -space-x-4">
          {[1,2,3,4].map(i => (
            <div key={i} className="w-10 h-10 rounded-full border-2 border-gray-900 bg-gray-500 overflow-hidden">
              <img src={`https://i.pravatar.cc/100?u=${i}`} alt="user" />
            </div>
          ))}
          <div className="w-10 h-10 rounded-full border-2 border-gray-900 bg-black flex items-center justify-center text-[10px] font-bold">
            +12
          </div>
        </div>
      </div>
      
      <div className="brutalist-border bg-[#ff4400] p-8 flex flex-col justify-end">
        <h5 className="text-4xl font-black text-white leading-none mb-4 uppercase">Submit Your Piece</h5>
        <button className="w-full bg-white text-black py-4 font-black uppercase flex items-center justify-center gap-2">
          Get Featured <Send className="w-4 h-4" />
        </button>
      </div>
    </div>

  </div>
</div>

const Footer = () => (
<div className="relative z-10 w-full max-w-7xl pb-20">
  <div className="grid grid-cols-1 md:grid-cols-2 gap-20 items-end border-b border-white/20 pb-20">
    <div>
      <h2 className="kinetic-text text-6xl md:text-[10rem] text-white opacity-20 leading-none">
        COLLABORATE
      </h2>
      <div className="mt-12 flex flex-wrap gap-8 text-white font-bold uppercase tracking-widest text-sm">
        <a href="#" className="hover:text-[#c4ff00]">Newsletter</a>
        <a href="#" className="hover:text-[#c4ff00]">Privacy</a>
        <a href="#" className="hover:text-[#c4ff00]">Terms</a>
        <a href="#" className="hover:text-[#c4ff00]">Twitter</a>
        <a href="#" className="hover:text-[#c4ff00]">Instagram</a>
      </div>
    </div>

    <div className="bg-white p-12 brutalist-border">
      <h3 className="text-black font-black text-3xl uppercase mb-6">Stay In The Loop</h3>
      <div className="flex border-b-2 border-black pb-4">
        <input 
          type="text" 
          placeholder="YOUR@EMAIL.COM" 
          className="bg-transparent flex-1 outline-none font-black placeholder:text-gray-300 text-black uppercase"
        />
        <button className="font-black uppercase text-sm hover:underline">Subscribe</button>
      </div>
    </div>
  </div>
  
  <div className="pt-10 flex justify-between items-center text-xs text-white/40 font-bold tracking-widest uppercase">
    <span>© 2026 ART_COLLECTIVE GLOBAL</span>
    <div className="flex gap-4 items-center">
      <Globe className="w-4 h-4" />
      <span>EN / GLOBAL</span>
    </div>
  </div>
</div>

export default function App() {
return (
<div className="bg-white font-sans text-black selection:bg-[#c4ff00] selection:text-black">
<style>{styles}</style>
<Nav />
<main>
<Hero />
<UserGalleryPortal />
<EventsSection />
<ArtistSpotlight />
</main>
<Footer />
</div>
);
}
import React, { useState, useEffect } from 'react';
import {
Menu, X, ArrowRight, Calendar, Users,
Palette, FileText, Send, Globe, ChevronRight,
Sparkles, Zap, Search, Filter, Camera, PenTool,
MessageCircle, Share2, Heart
} from 'lucide-react';
/**
•  ARCHITECTURAL PLAN:
•  State-driven navigation (Switch/Case)
•  A: Spatial/Glass/Fluid
•  B: Brutalist/Kinetic/High-Contrast
*/
const styles = @keyframes fluid { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } } .mesh-gradient { background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab); background-size: 400% 400%; animation: fluid 15s ease infinite; } .glass-card { background: rgba(255, 255, 255, 0.05); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.1); } .brutalist-border { border: 3px solid black; box-shadow: 8px 8px 0px 0px rgba(0,0,0,1); } .kinetic-text { font-family: 'Inter', sans-serif; font-weight: 900; text-transform: uppercase; letter-spacing: -0.05em; line-height: 0.85; } .glitch-hover:hover { animation: glitch 0.3s cubic-bezier(.25,.46,.45,.94) both infinite; } @keyframes glitch { 0% { transform: translate(0) } 20% { transform: translate(-2px, 2px) } 40% { transform: translate(-2px, -2px) } 60% { transform: translate(2px, 2px) } 80% { transform: translate(2px, -2px) } 100% { transform: translate(0) } };
// --- SHARED COMPONENTS ---
const Nav = ({ setPage, currentPage }) => (
  <div className="hidden md:flex gap-8 text-[10px] font-black uppercase tracking-widest text-black">
    {['home', 'artists', 'projects', 'submit'].map((p) => (
      <button 
        key={p} 
        onClick={() => setPage(p)}
        className={`${currentPage === p ? 'line-through' : 'hover:line-through'} transition-all`}
      >
        {p}
      </button>
    ))}
  </div>

  <button className="bg-black text-white px-5 py-2 rounded-full text-xs font-black uppercase hover:scale-105 transition-transform">
    Connect
  </button>
</div>

const Footer = () => (
// --- PAGE COMPONENTS ---
const HomePage = ({ setPage }) => (
{/* QUICK PORTAL: Style B + Structure A */}
<section className="py-20 px-4">
  <div className="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8">
    <div onClick={() => setPage('submit')} className="group brutalist-border h-[400px] relative overflow-hidden cursor-pointer bg-purple-600">
      <div className="absolute inset-0 mesh-gradient opacity-50 group-hover:opacity-100 transition-opacity" />
      <div className="absolute inset-0 flex items-center justify-center z-10">
        <h2 className="text-white font-black text-6xl uppercase tracking-tighter group-hover:scale-110 transition-transform">Submit Art</h2>
      </div>
    </div>
    <div className="glass-card p-12 flex flex-col justify-center">
      <h3 className="text-4xl font-black uppercase mb-4">The Collective Feed</h3>
      <p className="text-gray-600 mb-8 font-medium">Real-time updates from our global nodes. 24 new pieces uploaded in the last hour.</p>
      <div className="flex gap-2">
        {[1,2,3,4,5].map(i => (
          <div key={i} className="w-12 h-12 bg-gray-200 brutalist-border !border-2 overflow-hidden">
            <img src={`https://picsum.photos/100/100?random=${i}`} alt="feed" />
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

const ArtistsPage = () => {
const artists = [
{ name: "SOPHIE_V", tags: ["3D", "NEON"], img: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&q=80&w=400" },
{ name: "KAI_ZEN", tags: ["MINIMAL", "WEBGL"], img: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&q=80&w=400" },
{ name: "ECHO_P", tags: ["POETRY", "SOUND"], img: "https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?auto=format&fit=crop&q=80&w=400" },
{ name: "NOVA_X", tags: ["NFT", "KINETIC"], img: "https://images.unsplash.com/photo-1534528741775-53994a69daeb?auto=format&fit=crop&q=80&w=400" },
];
return (
<div className="pt-32 pb-20 px-4 animate-in slide-in-from-bottom duration-500">
<div className="max-w-7xl mx-auto">
{/* HEADER: A+B Rotational */}
<div className="flex flex-col md:flex-row justify-between items-end mb-16 gap-8">
<h1 className="kinetic-text text-7xl md:text-9xl leading-none">The<br/>Visionaries</h1>
<div className="flex gap-4 w-full md:w-auto">
<div className="flex-1 brutalist-border bg-white flex items-center px-4 py-2">
<Search className="w-4 h-4 mr-2" />
<input type="text" placeholder="FILTER BY NODE..." className="bg-transparent outline-none font-bold text-xs uppercase w-full" />
</div>
<button className="bg-black text-white p-4 brutalist-border !shadow-none">
<Filter className="w-5 h-5" />
</button>
</div>
</div>
    {/* GRID: Structure A (Bento) + Style B (Raw/Noise) */}
    <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
      {artists.map((artist, idx) => (
        <div key={idx} className={`group relative h-[500px] overflow-hidden ${idx % 3 === 0 ? 'md:col-span-2' : ''}`}>
          <div className="absolute inset-0 bg-black z-10 opacity-0 group-hover:opacity-40 transition-opacity" />
          <img src={artist.img} className="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-700" alt={artist.name} />
          <div className="absolute bottom-0 left-0 w-full p-8 z-20 translate-y-full group-hover:translate-y-0 transition-transform">
            <div className="glass-card p-6 text-white border-white/30 backdrop-blur-md">
              <div className="flex justify-between items-center mb-2">
                <h3 className="text-3xl font-black italic">{artist.name}</h3>
                <Heart className="w-5 h-5 hover:fill-red-500 cursor-pointer" />
              </div>
              <div className="flex gap-2">
                {artist.tags.map(t => <span key={t} className="text-[8px] font-black tracking-widest bg-white text-black px-2 py-1">{t}</span>)}
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>

);
};
const ProjectsPage = () => (
  {/* Structure B: Broken Grid */}
  <div className="space-y-12">
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
      <div className="lg:col-span-7 brutalist-border p-1">
        <img src="https://images.unsplash.com/photo-1550684848-fac1c5b4e853?auto=format&fit=crop&q=80&w=1200" className="w-full h-[400px] object-cover" alt="mural" />
      </div>
      <div className="lg:col-span-5 relative -ml-0 lg:-ml-24 z-10">
        <div className="glass-card p-12 shadow-2xl bg-white/80">
          <span className="text-[#e73c7e] font-black uppercase text-[10px] tracking-[0.3em] mb-4 block">ACTIVE COLLAB / 04 SPOTS LEFT</span>
          <h2 className="text-5xl font-black uppercase mb-6 leading-none">Metropolitan<br/>Synth-Mural</h2>
          <p className="text-gray-600 mb-8 font-medium">Join 12 other artists in creating the first AR-integrated mural in downtown Berlin. Deadline: Oct 26.</p>
          <button className="w-full bg-black text-white py-5 font-black uppercase flex items-center justify-center gap-4 hover:bg-white hover:text-black transition-all border-2 border-black">
            Join Research <MessageCircle className="w-5 h-5" />
          </button>
        </div>
      </div>
    </div>

    <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
      <div className="lg:col-span-5 order-2 lg:order-1 relative -mr-0 lg:-mr-24 z-10">
        <div className="glass-card p-12 shadow-2xl bg-black text-white">
          <span className="text-[#23d5ab] font-black uppercase text-[10px] tracking-[0.3em] mb-4 block">MENTORSHIP / OPEN</span>
          <h2 className="text-5xl font-black uppercase mb-6 leading-none">Generative<br/>Poetry Bot</h2>
          <p className="text-gray-400 mb-8 font-medium">Developing a custom LLM fine-tuned on modern abstract poetry. Seeking Python developers and writers.</p>
          <button className="w-full bg-[#c4ff00] text-black py-5 font-black uppercase flex items-center justify-center gap-4 brutalist-border !shadow-none hover:translate-x-1 hover:translate-y-1 transition-all">
            Access Node <Zap className="w-5 h-5" />
          </button>
        </div>
      </div>
      <div className="lg:col-span-7 order-1 lg:order-2 brutalist-border p-1 bg-white">
        <img src="https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?auto=format&fit=crop&q=80&w=1200" className="w-full h-[400px] object-cover opacity-80" alt="code art" />
      </div>
    </div>
  </div>
</div>

const SubmitPage = () => (
  <form className="space-y-8" onSubmit={(e) => e.preventDefault()}>
    <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div className="space-y-2">
        <label className="text-[10px] font-black uppercase tracking-widest">Artist Handle</label>
        <input type="text" placeholder="@" className="w-full border-b-2 border-black p-4 outline-none focus:bg-gray-50 transition-colors font-bold" />
      </div>
      <div className="space-y-2">
        <label className="text-[10px] font-black uppercase tracking-widest">Medium</label>
        <select className="w-full border-b-2 border-black p-4 outline-none focus:bg-gray-50 transition-colors font-bold bg-transparent">
          <option>VISUAL ART</option>
          <option>POETRY / WRITING</option>
          <option>DIGITAL / 3D</option>
          <option>AUDIO / PERFORMANCE</option>
        </select>
      </div>
    </div>

    <div className="space-y-2">
      <label className="text-[10px] font-black uppercase tracking-widest">The Narrative</label>
      <textarea placeholder="DESCRIBE THE ESSENCE..." rows="4" className="w-full border-b-2 border-black p-4 outline-none focus:bg-gray-50 transition-colors font-bold resize-none" />
    </div>

    <div className="group brutalist-border h-[200px] border-dashed flex flex-col items-center justify-center cursor-pointer hover:bg-gray-50 transition-all">
      <Camera className="w-10 h-10 mb-4 text-gray-400 group-hover:text-black group-hover:scale-110 transition-all" />
      <span className="font-black uppercase text-xs">Drop Assets Here (Max 50MB)</span>
    </div>

    <button className="w-full bg-black text-white py-6 font-black uppercase text-2xl hover:bg-[#c4ff00] hover:text-black transition-all group relative overflow-hidden">
      <span className="relative z-10">Broadcast to Node</span>
      <div className="absolute top-0 -left-full w-full h-full bg-[#c4ff00] group-hover:left-0 transition-all duration-500 ease-in-out -z-0" />
    </button>
  </form>
</div>

// --- MAIN APP ENTRY ---
export default function App() {
const [page, setPage] = useState('home');
useEffect(() => {
window.scrollTo({ top: 0, behavior: 'smooth' });
}, [page]);
const renderPage = () => {
switch(page) {
case 'home': return <HomePage setPage={setPage} />;
case 'artists': return <ArtistsPage />;
case 'projects': return <ProjectsPage />;
case 'submit': return <SubmitPage />;
default: return <HomePage setPage={setPage} />;
}
};
return (
<div className="bg-white min-h-screen selection:bg-[#c4ff00] selection:text-black">
<style>{styles}</style>
<Nav setPage={setPage} currentPage={page} />
  <main className="transition-all duration-500">
    {renderPage()}
  </main>

  <Footer />
</div>

);
}
import React, { useState, useEffect, useRef } from 'react';
import {
Menu, X, ArrowRight, Calendar, Users,
Palette, FileText, Send, Globe, ChevronRight,
Sparkles, Zap, Search, Filter, Camera, PenTool,
MessageCircle, Share2, Heart, Clock, Video, Monitor,
Loader2, RefreshCcw, Download
} from 'lucide-react';
/**
•  NEW NODES:
•  1.  THE FORGE: Live asset generation using Imagen-4.
•  2.  THE CALENDAR: Webinar & Consultation booking.
•  •  CONTEXTUAL AI STRATEGY:
•  Images generated represent "Latent Collective Memories"
•  Styles: "Organic Digitalism" (A) or "Neo-Brutalist Collage" (B).
*/
const styles = @keyframes fluid { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } } .mesh-gradient { background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab); background-size: 400% 400%; animation: fluid 15s ease infinite; } .glass-card { background: rgba(255, 255, 255, 0.05); backdrop-filter: blur(16px); border: 1px solid rgba(255, 255, 255, 0.1); } .brutalist-border { border: 3px solid black; box-shadow: 8px 8px 0px 0px rgba(0,0,0,1); } .kinetic-text { font-family: 'Inter', sans-serif; font-weight: 900; text-transform: uppercase; letter-spacing: -0.05em; line-height: 0.85; } .pixel-grid { background-image: radial-gradient(rgba(0,0,0,0.1) 1px, transparent 1px); background-size: 20px 20px; };
// --- API HELPERS ---
const apiKey = "";
const generateImage = async (prompt) => {
const url = https://generativelanguage.googleapis.com/v1beta/models/imagen-4.0-generate-001:predict?key=${apiKey};
const payload = { instances: { prompt: An abstract, avant-garde art piece for a digital collective, ${prompt}, neo-brutalist collage style, high grain, sophisticated chaos, glass textures }, parameters: { sampleCount: 1 } };
let delay = 1000;
for (let i = 0; i < 5; i++) {
try {
const response = await fetch(url, { method: 'POST', body: JSON.stringify(payload) });
if (!response.ok) throw new Error('Generation failed');
const data = await response.json();
return data:image/png;base64,${data.predictions[0].bytesBase64Encoded};
} catch (e) {
if (i === 4) throw e;
await new Promise(resolve => setTimeout(resolve, delay));
delay *= 2;
}
}
};
// --- COMPONENTS ---
const Nav = ({ setPage, currentPage }) => (
  <div className="hidden md:flex gap-6 text-[10px] font-black uppercase tracking-widest text-black">
    {['home', 'artists', 'forge', 'events', 'submit'].map((p) => (
      <button key={p} onClick={() => setPage(p)} className={`${currentPage === p ? 'line-through text-[#e73c7e]' : 'hover:line-through'} transition-all`}>
        {p}
      </button>
    ))}
  </div>

  <button className="bg-black text-white px-5 py-2 rounded-full text-xs font-black uppercase hover:scale-105 transition-transform">
    Connect Node
  </button>
</div>

const ForgePage = () => {
const [prompt, setPrompt] = useState("");
const [img, setImg] = useState(null);
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);
const handleForge = async () => {
if (!prompt) return;
setLoading(true);
setError(null);
try {
const result = await generateImage(prompt);
setImg(result);
} catch (e) {
setError("NEURAL LINK FAILED. RETRY BROADCAST.");
} finally {
setLoading(false);
}
};
return (
<div className="pt-32 pb-20 px-4 min-h-screen bg-gray-50 pixel-grid">
<div className="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-12 gap-12">
    {/* CONTROL PANEL (Style B) */}
    <div className="lg:col-span-4 space-y-8">
      <div className="brutalist-border bg-black text-white p-8">
        <h1 className="kinetic-text text-6xl mb-4">The Forge</h1>
        <p className="text-xs font-bold uppercase tracking-widest text-[#c4ff00]">Neural Asset Generator // v4.0</p>
      </div>

      <div className="glass-card p-8 bg-white/50 space-y-6">
        <div className="space-y-2">
          <label className="text-[10px] font-black uppercase">Input Latent Prompt</label>
          <textarea 
            value={prompt}
            onChange={(e) => setPrompt(e.target.value)}
            placeholder="e.g., Shattered glass, neon rain, abstract organic forms..."
            className="w-full h-32 bg-transparent border-b-2 border-black p-4 outline-none font-bold uppercase resize-none text-sm"
          />
        </div>
        
        <button 
          onClick={handleForge}
          disabled={loading}
          className="w-full bg-black text-white py-4 font-black uppercase flex items-center justify-center gap-4 hover:bg-[#c4ff00] hover:text-black transition-all group"
        >
          {loading ? <Loader2 className="animate-spin" /> : <Zap className="group-hover:fill-current" />}
          {loading ? "Synthesizing..." : "Initiate Forge"}
        </button>
        {error && <p className="text-red-600 font-black text-[10px] text-center">{error}</p>}
      </div>

      <div className="brutalist-border bg-white p-6">
        <h3 className="font-black uppercase text-xs mb-4">Neural Parameters</h3>
        <div className="space-y-4">
          {['Chaos Level', 'Grain Density', 'Spatial Depth'].map(label => (
            <div key={label} className="flex justify-between items-center">
              <span className="text-[10px] font-bold">{label}</span>
              <div className="w-24 h-2 bg-gray-100"><div className="w-1/2 h-full bg-black" /></div>
            </div>
          ))}
        </div>
      </div>
    </div>

    {/* OUTPUT CANVAS (Style A) */}
    <div className="lg:col-span-8">
      <div className="relative aspect-square md:aspect-video brutalist-border bg-gray-900 overflow-hidden group">
        {!img && !loading && (
          <div className="absolute inset-0 flex flex-col items-center justify-center text-white/20">
            <Monitor className="w-20 h-20 mb-4" />
            <span className="font-black uppercase tracking-[0.5em] text-sm">Waiting for Neural Stream</span>
          </div>
        )}
        
        {loading && (
          <div className="absolute inset-0 mesh-gradient opacity-30 flex items-center justify-center">
            <div className="text-center">
              <div className="w-20 h-20 border-4 border-black border-t-transparent rounded-full animate-spin mx-auto mb-4" />
              <p className="font-black text-black uppercase tracking-widest text-xs animate-pulse">Mapping Latent Space...</p>
            </div>
          </div>
        )}

        {img && (
          <>
            <img src={img} className="w-full h-full object-cover animate-in fade-in zoom-in duration-1000" alt="Generated art" />
            <div className="absolute bottom-6 right-6 flex gap-2">
              <button className="p-4 bg-white brutalist-border !shadow-none hover:bg-black hover:text-white transition-all"><Download /></button>
              <button className="p-4 bg-white brutalist-border !shadow-none hover:bg-black hover:text-white transition-all"><Share2 /></button>
            </div>
          </>
        )}
      </div>
      
      <div className="mt-8 grid grid-cols-4 gap-4">
         {[1,2,3,4].map(i => (
           <div key={i} className="aspect-square glass-card rounded-xl border-dashed border-2 border-black/20 flex items-center justify-center hover:bg-white transition-all cursor-pointer">
             <RefreshCcw className="text-black/10 w-6 h-6" />
           </div>
         ))}
      </div>
    </div>
  </div>
</div>

);
};
const EventsPage = () => (
  <div className="grid grid-cols-1 lg:grid-cols-12 gap-12">
    {/* MAIN EVENTS - Structure B */}
    <div className="lg:col-span-8 space-y-12">
      <div className="brutalist-border bg-[#c4ff00] p-12 flex flex-col md:flex-row gap-12 group cursor-pointer hover:bg-white transition-colors">
        <div className="w-full md:w-1/3 aspect-square glass-card rounded-none overflow-hidden border-2 border-black">
          <img src="https://images.unsplash.com/photo-1542744173-8e7e5381c619?auto=format&fit=crop&q=80&w=400" className="w-full h-full object-cover" />
        </div>
        <div className="flex-1 flex flex-col justify-between">
          <div>
            <span className="bg-black text-white px-3 py-1 text-[10px] font-black uppercase mb-4 inline-block">Free Webinar // Oct 28</span>
            <h2 className="text-5xl font-black uppercase leading-none mb-4">Mastering<br/>Latent Spaces</h2>
            <p className="font-medium text-gray-800">Learn to use generative tools to expand your traditional practice. 2-hour intensive.</p>
          </div>
          <button className="mt-8 self-start flex items-center gap-4 font-black uppercase group-hover:gap-6 transition-all">
            Book My Spot <ArrowRight />
          </button>
        </div>
      </div>

      <div className="brutalist-border bg-black text-white p-12 flex flex-col md:flex-row gap-12 group cursor-pointer hover:bg-gray-900 transition-colors">
        <div className="w-full md:w-1/3 aspect-square glass-card rounded-none overflow-hidden border-2 border-white/20">
          <img src="https://images.unsplash.com/photo-1522071823912-326966838a6a?auto=format&fit=crop&q=80&w=400" className="w-full h-full object-cover" />
        </div>
        <div className="flex-1 flex flex-col justify-between">
          <div>
            <span className="bg-[#e73c7e] text-white px-3 py-1 text-[10px] font-black uppercase mb-4 inline-block">1-on-1 // Design Consult</span>
            <h2 className="text-5xl font-black uppercase leading-none mb-4">Portfolio<br/>Disruption</h2>
            <p className="font-medium text-gray-400">Critical review of your current trajectory with senior nodes. Tailored advice.</p>
          </div>
          <button className="mt-8 self-start flex items-center gap-4 font-black uppercase text-[#e73c7e]">
            Schedule Call <ArrowRight />
          </button>
        </div>
      </div>
    </div>

    {/* SIDEBAR - Structure A */}
    <div className="lg:col-span-4 space-y-8">
      <div className="glass-card p-8 bg-gray-50 border-black/10">
        <h3 className="font-black uppercase text-sm mb-6 flex items-center gap-2">
          <Clock className="w-4 h-4" /> Node Timeline
        </h3>
        <div className="space-y-6">
          {[
            { time: '14:00', event: 'Node Check-in', type: 'Daily' },
            { time: '16:30', event: 'WebGL Workshop', type: 'Pro' },
            { time: '19:00', event: 'Artist Mixer', type: 'Social' }
          ].map((item, idx) => (
            <div key={idx} className="flex gap-4 items-start pb-6 border-b border-black/5">
              <span className="font-black text-xs text-[#e73c7e]">{item.time}</span>
              <div>
                <h4 className="font-black uppercase text-xs">{item.event}</h4>
                <p className="text-[10px] font-bold text-gray-400 uppercase">{item.type}</p>
              </div>
            </div>
          ))}
        </div>
      </div>

      <div className="brutalist-border p-8 bg-white">
        <Video className="w-10 h-10 mb-4" />
        <h3 className="font-black uppercase text-xl leading-none mb-4">Archive<br/>Access</h3>
        <p className="text-xs font-medium text-gray-600 mb-6">Missed a sync? Access all previous recordings in the node vault.</p>
        <button className="text-xs font-black uppercase underline decoration-2 underline-offset-4">Enter Vault</button>
      </div>
    </div>
  </div>
</div>

// --- MAIN APP ---
export default function App() {
const [page, setPage] = useState('home');
useEffect(() => {
window.scrollTo({ top: 0, behavior: 'smooth' });
}, [page]);
const renderPage = () => {
switch(page) {
case 'forge': return <ForgePage />;
case 'events': return <EventsPage />;
default: return <div className="pt-32 p-4 text-center kinetic-text text-8xl">Page {page} coming...</div>;
}
};
return (
<div className="bg-white min-h-screen selection:bg-[#c4ff00] selection:text-black">
<style>{styles}</style>
<Nav setPage={setPage} currentPage={page} />
<main>
{page === 'home' ? (
<div className="pt-32 text-center space-y-20">
<h1 className="kinetic-text text-9xl">Collective<br/>OS</h1>
<div className="flex justify-center gap-8">
<button onClick={() => setPage('forge')} className="brutalist-border bg-black text-white p-8 group">
<h2 className="text-4xl font-black uppercase mb-2">The Forge</h2>
<p className="text-[10px] font-bold group-hover:text-[#c4ff00]">LIVE ASSET GENERATION</p>
</button>
<button onClick={() => setPage('events')} className="brutalist-border bg-[#c4ff00] text-black p-8 group">
<h2 className="text-4xl font-black uppercase mb-2">Events</h2>
<p className="text-[10px] font-bold">NODE SYNC & WEBINARS</p>
</button>
</div>
</div>
) : renderPage()}
</main>
<footer className="mt-32 p-20 bg-black text-white/20 font-black uppercase tracking-widest text-center text-[10rem] opacity-5 overflow-hidden whitespace-nowrap">
Art Collective v4.0.0.0.0.0.1
</footer>
</div>
);
}
