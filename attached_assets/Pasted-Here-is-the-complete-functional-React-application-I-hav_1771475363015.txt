Here is the complete, functional React application. I have expanded the code to include additional "toolkit" features, refined animations, and a more robust state management system for the demonstration.
You can copy-paste this directly into a React environment (like Create React App, Next.js, or Vite) that has lucide-react and tailwindcss installed.
import React, { useState, useEffect, useMemo } from 'react';
import {
ArrowRight,
Layers,
Cpu,
Zap,
Layout,
Menu,
X,
ChevronRight,
Monitor,
Database,
ShieldCheck,
Code2,
Box,
Activity,
Server,
Terminal,
Fingerprint,
Workflow,
GitBranch,
CheckCircle2,
AlertTriangle,
Sparkles,
Globe,
Lock
} from 'lucide-react';
// --- Design System Constants ---
const COLORS = {
violet: '#7c3aed',
coral: '#ff6b6b',
cyan: '#22d3ee',
ink: '#0f172a',
cream: '#fef3c7',
green: '#a3e635',
gold: '#fbbf24',
rose: '#fb7185',
blue: '#3b82f6'
};
// --- Visual Effects Components ---
const RisographTexture = () => (
<svg className="absolute inset-0 w-full h-full opacity-[0.05] pointer-events-none mix-blend-multiply z-0">
<filter id="noise">
<feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
<feColorMatrix type="saturate" values="0" />
</filter>
<rect width="100%" height="100%" filter="url(#noise)" />
</svg>
);
const MemphisShapes = () => (
const GridBackground = () => (
// --- Toolkit & Feature Components ---
const TerminalWidget = ({ logs }) => (
const MetricCard = ({ label, value, trend, color }) => (
// --- Pages ---
const LandingPage = ({ onNavigate }) => {
const projects = [
{
id: 'project-detail',
title: "Enterprise Brand Governance",
tag: "Governance Engine",
description: "A structured flow visualization framework that channels chaotic inputs into aligned, energetic outputs.",
color: COLORS.coral,
icon: <ShieldCheck size={32} />
},
{
id: 'prompts',
title: "Prompt Engineering Framework",
tag: "LLM Orchestration",
description: "Modular component library for language structuring. A systematic grid of geometric primitives.",
color: COLORS.cyan,
icon: <Terminal size={32} />
}
];
return (
<div className="animate-in fade-in duration-700">
<header className="relative pt-32 pb-20 px-6 overflow-hidden bg-[#0f172a] min-h-screen flex items-center">
<GridBackground />
<div className="container mx-auto relative z-10">
<div className="grid lg:grid-cols-12 gap-12 items-center">
<div className="lg:col-span-7">
<span className="inline-block bg-[#ff6b6b] text-[#0f172a] px-4 py-1 text-xs font-black uppercase tracking-widest mb-6 shadow-[4px_4px_0px_0px_rgba(255,255,255,0.2)]">
ARCHITECTING THE FUTURE
</span>
<h1 className="text-5xl sm:text-6xl md:text-8xl font-black text-[#fef3c7] uppercase italic leading-[0.9] mb-8 break-words">
Designing <span className="text-[#22d3ee]">systems</span> <br />
not just <span className="text-[#fbbf24]">software</span>.
</h1>
<p className="text-xl text-slate-400 max-w-xl font-medium leading-relaxed mb-10">
Senior AI Architect specializing in enterprise brand governance and multi-engine orchestration.
Memphis design meets systems architecture.
</p>
<div className="flex flex-wrap gap-4">

</button>
<button
onClick={() => onNavigate('systems')}
className="border-4 border-[#22d3ee] text-[#22d3ee] px-8 py-4 text-sm font-black uppercase tracking-widest hover:bg-[#22d3ee] hover:text-[#0f172a] transition-all">
Systems Logic
</button>
</div>
</div>
        <div className="lg:col-span-5 relative hidden lg:block">
          <div className="aspect-square bg-[#fef3c7] border-[12px] border-[#fbbf24] relative flex items-center justify-center overflow-hidden rotate-3 shadow-2xl hover:rotate-0 transition-transform duration-500">
             <div className="w-full h-full relative p-8">
                <div className="absolute bottom-0 left-0 w-3/4 h-1/2 bg-[#7c3aed] transform -skew-x-12 opacity-80" />
                <div className="absolute top-1/4 right-0 w-1/2 h-1/2 bg-[#22d3ee] transform skew-y-12 opacity-80" />
                <div className="absolute top-0 left-1/4 w-1/3 h-1/3 bg-[#fef3c7] border-4 border-[#0f172a] z-10 flex items-center justify-center animate-bounce">
                  <Zap size={40} className="text-[#ff6b6b]" />
                </div>
                <div className="absolute top-1/2 left-1/2 w-4 h-full bg-[#fbbf24] -translate-x-1/2 -translate-y-1/2 rotate-45" />
             </div>
            <RisographTexture />
          </div>
        </div>
      </div>
    </div>
    <MemphisShapes />
  </header>

  <section className="py-24 px-6 bg-[#fef3c7] relative">
    <div className="container mx-auto relative z-10">
      <h2 className="text-4xl md:text-6xl font-black text-[#0f172a] uppercase italic mb-16 tracking-tighter">
        Core <span className="text-[#ff6b6b]">Interventions</span>
      </h2>
      <div className="grid md:grid-cols-2 gap-12">
        {projects.map((p) => (
          <div 
            key={p.id} 
            onClick={() => onNavigate(p.id)}
            className="group cursor-pointer bg-[#0f172a] p-1 border-4 border-[#0f172a] hover:border-[#a3e635] transition-all hover:-translate-y-2"
          >
            <div className="aspect-video bg-white relative overflow-hidden flex items-center justify-center">
              <div className="absolute inset-0 opacity-10" style={{ backgroundColor: p.color }} />
              <div className="w-24 h-24 sm:w-32 sm:h-32 border-8 border-[#0f172a] rounded-full flex items-center justify-center group-hover:scale-110 transition-transform bg-[#fef3c7]">
                <div style={{ color: p.color }}>{p.icon}</div>
              </div>
              <RisographTexture />
            </div>
            <div className="p-6 sm:p-8 text-[#fef3c7]">
              <span className="text-[10px] font-black tracking-widest uppercase mb-4 block" style={{ color: p.color }}>{p.tag}</span>
              <h3 className="text-2xl sm:text-3xl font-black uppercase mb-4">{p.title}</h3>
              <p className="text-sm sm:text-base text-slate-400 mb-6">{p.description}</p>
              <div className="flex items-center gap-2 text-xs font-black uppercase text-[#a3e635]">
                Open Specification <ArrowRight size={14} />
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</div>

);
};
const SystemsPage = ({ onNavigate }) => {
const cards = [
{
id: 'ingestion-detail',
icon: <Database />,
title: "Ingestion Layers",
color: COLORS.violet,
desc: "High-throughput data normalization using Memphis-style grouping."
},
{
id: 'inference-detail',
icon: <Cpu />,
title: "Inference Engines",
color: COLORS.coral,
desc: "Multi-model orchestration with weighted bias control."
},
{
id: 'governance-detail',
icon: <ShieldCheck />,
title: "Governance Walls",
color: COLORS.green,
desc: "Automated brand alignment checks via deterministic logic."
}
];
return (
<div className="pt-32 pb-24 px-6 bg-[#0f172a] text-[#fef3c7] min-h-screen animate-in slide-in-from-bottom-10 duration-700">
<div className="container mx-auto">
<div className="mb-20">
<span className="text-[#a3e635] font-black uppercase tracking-widest text-sm">ARCHITECTURAL ONTOLOGY</span>
<h1 className="text-5xl sm:text-6xl md:text-9xl font-black uppercase italic leading-none mt-4 break-words">
The <span className="text-[#22d3ee]">Logic</span> <br />
of Flow.
</h1>
</div>
    <div className="grid lg:grid-cols-3 gap-12">
      {cards.map((item, i) => (
        <div 
          key={i} 
          onClick={() => onNavigate(item.id)}
          className="border-4 border-[#fef3c7]/20 p-8 sm:p-10 hover:border-[#fbbf24] transition-all relative overflow-hidden group cursor-pointer bg-[#0f172a]"
        >
          <div className="mb-8 group-hover:scale-110 transition-transform duration-500" style={{ color: item.color }}>
            {React.cloneElement(item.icon, { size: 48 })}
          </div>
          <h3 className="text-2xl font-black uppercase mb-4">{item.title}</h3>
          <p className="text-slate-400 leading-relaxed mb-8">{item.desc}</p>
          <div className="flex items-center gap-2 text-xs font-black uppercase tracking-widest text-[#fbbf24]">
            Explore Layer <ArrowRight size={14} />
          </div>
          <RisographTexture />
        </div>
      ))}
    </div>

    <div className="mt-32 p-6 sm:p-12 bg-[#fef3c7] text-[#0f172a] border-8 sm:border-[12px] border-[#fbbf24] relative overflow-hidden">
      <div className="relative z-10 flex flex-col md:flex-row items-center gap-12">
         <div className="w-full md:w-1/3">
            <div className="w-full aspect-square bg-[#0f172a] flex items-center justify-center p-8 relative overflow-hidden">
              <div className="absolute inset-0 opacity-20 bg-[ repeating-linear-gradient(45deg,#22d3ee_0,#22d3ee_10px,transparent_10px,transparent_20px)]" />
              <div className="relative w-full h-full border-2 border-[#22d3ee]">
                <div className="absolute inset-4 border-2 border-[#ff6b6b] rotate-45 animate-[spin_10s_linear_infinite]" />
                <div className="absolute inset-8 border-2 border-[#fbbf24] rounded-full animate-pulse" />
              </div>
            </div>
         </div>
         <div className="w-full md:w-2/3">
            <h2 className="text-3xl sm:text-4xl font-black uppercase italic mb-6 break-words leading-tight">Autonomous Orchestration</h2>
            <p className="text-lg sm:text-xl font-bold leading-relaxed mb-8">
              My systems don't just "process" — they decide. Using a tiered approach to model routing, we reduce latency by 40% while ensuring 100% brand safety across all LLM outputs.
            </p>
            <div className="flex flex-wrap gap-4">
              <div className="px-4 py-2 bg-[#0f172a] text-[#fef3c7] font-black text-xs uppercase tracking-tighter border border-[#0f172a]">Latency: 140ms</div>
              <div className="px-4 py-2 bg-[#ff6b6b] text-[#0f172a] font-black text-xs uppercase tracking-tighter border border-[#0f172a]">Reliability: 99.9%</div>
            </div>
         </div>
      </div>
      <RisographTexture />
    </div>
  </div>
</div>

);
};
const SystemDetailLayout = ({ title, subtitle, color, icon, content, stats }) => {
return (
<div className="pt-32 pb-24 min-h-screen bg-[#fef3c7] animate-in fade-in duration-500">
<div className="container mx-auto px-6">
<div className="grid lg:grid-cols-12 gap-12 mb-16">
<div className="lg:col-span-7">
<div className="flex items-center gap-4 mb-8">

 
</div>
</div>
</div>
    <div className="grid md:grid-cols-3 gap-8 mb-24">
      {stats.map((stat, i) => (
        <div key={i} className="bg-[#0f172a] p-8 text-[#fef3c7] relative overflow-hidden group border-2 border-[#0f172a] hover:border-[#fbbf24] transition-colors">
          <span className="block text-[10px] font-black uppercase tracking-widest text-slate-400 mb-2">{stat.label}</span>
          <span className="text-4xl font-black uppercase italic group-hover:text-[#a3e635] transition-colors">{stat.value}</span>
          <RisographTexture />
        </div>
      ))}
    </div>

    <div className="border-t-4 border-[#0f172a] pt-16">
      <h2 className="text-4xl font-black uppercase italic mb-12">Technical Framework</h2>
      <div className="grid lg:grid-cols-2 gap-16">
        {content.map((section, i) => (
          <div key={i} className="space-y-4">
            <h3 className="text-xl font-black uppercase flex items-center gap-3">
              <div className="w-4 h-4" style={{ backgroundColor: color }} />
              {section.heading}
            </h3>
            <p className="text-lg text-slate-600 leading-relaxed font-medium">
              {section.body}
            </p>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

);
};
const ProjectDetailPage = () => {
// Simulated live data for the toolkit
const [logs, setLogs] = useState([
{ time: '10:42:01', message: 'System initialized', type: 'info' },
{ time: '10:42:05', message: 'Connecting to vector store...', type: 'info' },
{ time: '10:42:06', message: 'Connection established', type: 'success' },
]);
useEffect(() => {
const interval = setInterval(() => {
const newLog = {
time: new Date().toLocaleTimeString('en-US', { hour12: false }),
message: ['Validating token schema...', 'Running semantic check...', 'Policy match found', 'Inference latency 45ms'][Math.floor(Math.random() * 4)],
type: ['info', 'success', 'warn'][Math.floor(Math.random() * 3)]
};
setLogs(prev => [...prev.slice(-5), newLog]);
}, 2500);
return () => clearInterval(interval);
}, []);
return (
<div className="pt-32 min-h-screen bg-[#fef3c7] animate-in fade-in duration-500">
<div className="container mx-auto px-6">
<div className="flex items-center gap-4 mb-8">
<div className="w-12 h-1 border-b-4 border-[#0f172a]" />
<span className="font-black uppercase tracking-widest text-sm italic">Case Study // 001</span>
</div>
    <h1 className="text-5xl sm:text-6xl md:text-8xl font-black uppercase italic text-[#0f172a] leading-none mb-12 break-words">
      Enterprise <br /> <span className="text-[#ff6b6b]">Brand Governance</span>
    </h1>

    <div className="grid lg:grid-cols-12 gap-12 mb-24">
      <div className="lg:col-span-8 space-y-8">
         {/* Visual Demo */}
         <div className="bg-[#0f172a] aspect-video relative flex items-center justify-center p-6 sm:p-12 overflow-hidden shadow-2xl border-4 border-[#0f172a]">
            <div className="w-full h-full border-4 border-[#22d3ee] flex items-center justify-center relative">
              <div className="relative w-1/2 aspect-square">
                <div className="absolute inset-0 bg-[#ff6b6b] rounded-full animate-pulse opacity-80" />
                <div className="absolute -inset-4 border-4 border-[#fbbf24] rotate-12 animate-[spin_5s_linear_infinite]" />
                <div className="absolute -inset-8 border-4 border-[#a3e635] -rotate-12 animate-[spin_7s_linear_infinite_reverse]" />
                <div className="absolute inset-0 flex items-center justify-center text-[#0f172a] font-black text-xl">
                  PROTECTED
                </div>
              </div>
            </div>
            <div className="absolute bottom-4 right-4 bg-[#fef3c7] px-4 py-2 font-mono text-[10px] text-[#0f172a] border border-[#0f172a]">
              STATUS: OPERATIONAL_V2.5
            </div>
            <RisographTexture />
         </div>

         {/* Expanded Toolkit Section */}
         <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
            <MetricCard label="Requests/min" value="14.2k" trend={12} color={COLORS.cyan} />
            <MetricCard label="Blocked" value="42" trend={-5} color={COLORS.coral} />
            <MetricCard label="Uptime" value="99.9%" trend={0} color={COLORS.green} />
         </div>
      </div>

      <div className="lg:col-span-4 space-y-8">
        <div className="p-6 bg-white border-2 border-[#0f172a] shadow-[8px_8px_0px_0px_rgba(15,23,42,1)]">
          <h3 className="text-xs font-black uppercase tracking-widest text-[#7c3aed] mb-4 flex items-center gap-2">
            <Terminal size={14} /> Live System Logs
          </h3>
          <TerminalWidget logs={logs} />
        </div>

        <div>
          <h3 className="text-xs font-black uppercase tracking-widest text-[#ff6b6b] mb-2">The Strategy</h3>
          <p className="text-lg font-bold text-[#0f172a] leading-snug">Implementation of a "Governance Filter" that runs deterministic semantic checks against a vector-based brand bible.</p>
        </div>

        <div className="p-8 bg-[#fbbf24] border-4 border-[#0f172a] shadow-[8px_8px_0px_0px_rgba(15,23,42,1)]">
          <h3 className="text-xs font-black uppercase tracking-widest text-[#0f172a] mb-4">Stack Details</h3>
          <div className="flex flex-wrap gap-2">
            {['TypeScript', 'Pinecone', 'GPT-4o', 'Anthropic', 'TensorFlow'].map(tag => (
              <span key={tag} className="bg-[#0f172a] text-[#fef3c7] px-2 py-1 text-[10px] font-bold uppercase border border-[#0f172a]">{tag}</span>
            ))}
          </div>
        </div>
      </div>
    </div>

    <div className="border-t-4 border-[#0f172a] pt-24 pb-32">
      <h2 className="text-3xl sm:text-4xl font-black uppercase italic mb-12">System Architecture</h2>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
        {[
          { label: "Input", color: COLORS.cyan, step: "Raw LLM Stream", icon: <Workflow /> },
          { label: "Filter", color: COLORS.violet, step: "Semantic Validation", icon: <Fingerprint /> },
          { label: "Check", color: COLORS.coral, step: "Constraint Matching", icon: <CheckCircle2 /> },
          { label: "Output", color: COLORS.green, step: "Signed Execution", icon: <Lock /> }
        ].map((node, i) => (
          <div key={i} className="relative pt-8 border-l-4 border-[#0f172a] pl-6 hover:bg-white p-4 transition-colors cursor-default">
            <div className="absolute -left-[14px] top-0 w-6 h-6 border-4 border-[#0f172a]" style={{ backgroundColor: node.color }} />
            <div className="mb-4 text-[#0f172a]">{node.icon}</div>
            <span className="block text-[10px] font-black uppercase tracking-widest mb-1 opacity-50">{node.label}</span>
            <span className="text-xl font-black uppercase italic">{node.step}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

);
};
// --- Main App Controller ---
const App = () => {
const [currentPage, setCurrentPage] = useState('home');
const [scrolled, setScrolled] = useState(false);
const [isMenuOpen, setIsMenuOpen] = useState(false);
useEffect(() => {
const handleScroll = () => setScrolled(window.scrollY > 50);
window.addEventListener('scroll', handleScroll);
return () => {
window.removeEventListener('scroll', handleScroll);
window.scrollTo(0, 0);
};
}, [currentPage]);
const navLinks = [
{ id: 'home', label: 'Systems' },
{ id: 'projects', label: 'Work' },
{ id: 'systems', label: 'Roadmap' },
];
const handleNavigate = (id) => {
setCurrentPage(id);
setIsMenuOpen(false);
window.scrollTo(0, 0);
};
return (

</button>
</div>
      <div className="flex flex-col gap-8">
        {navLinks.map((link) => (
          <button 
            key={link.id}
            onClick={() => handleNavigate(link.id)}
            className={`text-5xl font-black uppercase italic text-left tracking-tighter hover:translate-x-4 transition-transform ${currentPage === link.id ? 'text-[#ff6b6b]' : 'text-[#fef3c7]'}`}
          >
            {link.label}
          </button>
        ))}
      </div>
      
      <div className="mt-auto">
        <button className="w-full bg-[#fbbf24] text-[#0f172a] py-6 text-xl font-black uppercase tracking-widest shadow-[8px_8px_0px_0px_rgba(255,107,107,1)] hover:shadow-none hover:translate-y-1 transition-all">
          Hire Me
        </button>
      </div>
      <RisographTexture />
    </div>
  )}

  {/* Universal Nav */}
  <nav className={`fixed top-0 w-full z-50 transition-all duration-300 ${scrolled || currentPage !== 'home' ? 'bg-[#0f172a] py-4 shadow-lg' : 'bg-transparent py-8'}`}>
    <div className="container mx-auto px-6 flex justify-between items-center">
      <div 
        className="flex items-center gap-2 cursor-pointer group"
        onClick={() => handleNavigate('home')}
      >
        <div className="w-8 h-8 bg-[#7c3aed] flex items-center justify-center group-hover:rotate-90 transition-transform duration-300">
          <span className="text-[#fef3c7] font-black italic text-xl leading-none">A</span>
        </div>
        <span className={`font-black uppercase tracking-tighter text-xl ${scrolled || currentPage !== 'home' ? 'text-[#fef3c7]' : 'text-[#0f172a]'}`}>
          System<span className="text-[#ff6b6b]">Architect</span>
        </span>
      </div>
      
      {/* Desktop Nav */}
      <div className="hidden md:flex items-center gap-8">
        {navLinks.map((item) => (
          <button 
            key={item.id} 
            onClick={() => handleNavigate(item.id)}
            className={`text-xs font-bold uppercase tracking-widest hover:text-[#ff6b6b] transition-colors ${scrolled || currentPage !== 'home' ? 'text-[#fef3c7]' : 'text-[#0f172a]'} ${currentPage === item.id ? 'underline underline-offset-8 decoration-4 decoration-[#ff6b6b]' : ''}`}
          >
            {item.label}
          </button>
        ))}
        <button className="bg-[#fbbf24] text-[#0f172a] px-6 py-2 text-xs font-black uppercase tracking-widest hover:translate-x-1 hover:-translate-y-1 transition-transform shadow-[4px_4px_0px_0px_rgba(255,107,107,1)]">
          Hire Me
        </button>
      </div>
      
      {/* Mobile Menu Trigger */}
      <button onClick={() => setIsMenuOpen(true)} className="md:hidden p-2 hover:bg-white/10 rounded-lg transition-colors">
        <Menu size={28} className={`${scrolled || currentPage !== 'home' ? 'text-[#fef3c7]' : 'text-[#0f172a]'}`} />
      </button>
    </div>
  </nav>

  {/* Page Routing */}
  <main>
    {currentPage === 'home' && <LandingPage onNavigate={handleNavigate} />}
    {currentPage === 'projects' && <LandingPage onNavigate={handleNavigate} />}
    {currentPage === 'systems' && <SystemsPage onNavigate={handleNavigate} />}
    {currentPage === 'project-detail' && <ProjectDetailPage />}
    
    {currentPage === 'ingestion-detail' && (
      <SystemDetailLayout 
        title="Ingestion Layers"
        color={COLORS.violet}
        icon={<Database />}
        subtitle="High-throughput data normalization using Memphis-style grouping and semantic parsing."
        stats={[
          { label: "Throughput", value: "2.4GB/s" },
          { label: "Latency", value: "12ms" },
          { label: "Accuracy", value: "99.9%" }
        ]}
        content={[
          { heading: "Modular Normalization", body: "Every incoming data stream is treated as a geometric primitive—cleaned, reshaped, and tagged before it hits the vector store." },
          { heading: "Semantic Ingestion", body: "We use lightweight LLMs to tag incoming unstructured data at the source, ensuring our retrieval-augmented generation stays relevant." }
        ]}
      />
    )}

    {currentPage === 'inference-detail' && (
      <SystemDetailLayout 
        title="Inference Engines"
        color={COLORS.coral}
        icon={<Cpu />}
        subtitle="Dynamic multi-model orchestration with weighted bias control and cost-efficiency routing."
        stats={[
          { label: "Model Count", value: "12+" },
          { label: "Efficiency", value: "65%" },
          { label: "Decision T", value: "<40ms" }
        ]}
        content={[
          { heading: "Orchestration Logic", body: "Our routing engine determines the optimal model based on prompt complexity, cost constraints, and required semantic depth." },
          { heading: "Weighted Bias Control", body: "Fine-tune system outputs in real-time by adjusting internal weighting mechanisms without retraining the underlying models." }
        ]}
      />
    )}

    {currentPage === 'governance-detail' && (
      <SystemDetailLayout 
        title="Governance Walls"
        color={COLORS.green}
        icon={<ShieldCheck />}
        subtitle="Automated, deterministic brand safety and alignment checks built directly into the execution flow."
        stats={[
          { label: "Violation Detection", value: "100%" },
          { label: "Safety Tiers", value: "5 Levels" },
          { label: "Uptime", value: "99.99%" }
        ]}
        content={[
          { heading: "Deterministic Filtering", body: "We use hard-coded constraints alongside semantic checks to ensure that not a single word violates the brand's core values." },
          { heading: "Policy Enforcement", body: "The governance wall acts as a final gatekeeper, signing off on execution only after passing a 4-tiered validation pipeline." }
        ]}
      />
    )}
  </main>

  {/* Shared Footer */}
  <footer className="bg-[#ff6b6b] border-t-[12px] border-[#0f172a] py-20 px-6 relative overflow-hidden">
    <div className="container mx-auto text-center relative z-10">
      <div className="max-w-3xl mx-auto">
        <h2 className="text-5xl md:text-8xl font-black text-[#0f172a] uppercase italic leading-none mb-12">
          Ready to <br />
          <span className="bg-[#0f172a] text-[#fef3c7] px-4 inline-block transform -rotate-2">Orchestrate</span>?
        </h2>
        <div className="flex flex-wrap justify-center gap-6">
          <button className="bg-[#0f172a] text-[#fef3c7] px-10 py-5 text-sm font-black uppercase tracking-widest hover:scale-105 transition-transform border-2 border-[#0f172a]">
            Download CV
          </button>
          <button className="border-4 border-[#0f172a] text-[#0f172a] px-10 py-5 text-sm font-black uppercase tracking-widest hover:bg-[#0f172a] hover:text-[#fef3c7] transition-all">
            LinkedIn
          </button>
        </div>
      </div>
      
      <div className="mt-32 pt-12 border-t-2 border-[#0f172a]/20 flex flex-col md:flex-row justify-between items-center gap-8">
        <div className="flex items-center gap-2">
          <div className="w-6 h-6 bg-[#0f172a]" />
          <span className="font-black uppercase tracking-tighter text-lg text-[#0f172a]">
            ©2024 SystemArchitect
          </span>
        </div>
        <div className="flex gap-8">
          {['Twitter', 'GitHub', 'Read.cv'].map(social => (
            <a key={social} href="#" className="text-xs font-black uppercase tracking-widest text-[#0f172a] hover:underline decoration-2 underline-offset-4">
              {social}
            </a>
          ))}
        </div>
      </div>
    </div>
    <MemphisShapes />
  </footer>
</div>

);
};
export default App;